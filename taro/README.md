# [taro](https://github.com/NervJS/taro) in docker

## usage
```shell
docker run -it --network=host --name $(basename $(pwd))-taro -v $(pwd):/workspace registry-crs-huadong1.ctyun.cn/7sunarni/taro:v1  bash
```
